{
  "name": "postman-aws-spec-discovery-action",
  "version": "0.3.0",
  "description": "Public open-alpha Postman AWS API Gateway spec discovery for GitHub and GitLab workflows.",
  "type": "module",
  "main": "dist/index.cjs",
  "bin": {
    "postman-aws-spec-discovery": "dist/cli.cjs"
  },
  "scripts": {
    "build": "rm -rf dist && esbuild src/index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/index.cjs && esbuild src/cli.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/cli.cjs",
    "check:dist": "npm run build && git diff --exit-code -- dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "keywords": [
    "github-action",
    "postman",
    "aws",
    "apigateway",
    "openapi"
  ],
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@aws-sdk/client-api-gateway": "^3.1014.0",
    "@aws-sdk/client-apigatewayv2": "^3.1014.0",
    "@aws-sdk/client-sts": "^3.1014.0",
    "@smithy/node-http-handler": "^4.4.5"
  },
  "overrides": {
    "undici": "^6.24.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18",
    "yaml": "^2.8.2"
  }
}
