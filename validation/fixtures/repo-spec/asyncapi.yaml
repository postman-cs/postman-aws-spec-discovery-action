asyncapi: 2.6.0
info:
  title: Validation AsyncAPI
  version: 1.0.0
channels:
  validation-topic:
    subscribe:
      message:
        name: ValidationEvent
        payload:
          $ref: '#/components/schemas/ValidationEvent'
        examples:
          - name: sample-event
            payload:
              id: evt_123
components:
  schemas:
    ValidationEvent:
      type: object
      required:
        - id
      properties:
        id:
          type: string
