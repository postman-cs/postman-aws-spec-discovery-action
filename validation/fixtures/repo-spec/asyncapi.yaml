asyncapi: 2.6.0
info:
  title: Validation AsyncAPI
  version: 1.0.0
channels:
  validation-topic:
    subscribe:
      message:
        payload:
          type: object
          properties:
            id:
              type: string
